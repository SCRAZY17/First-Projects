/** 
 *el operador '?' pregunta si el el valor ingresado es 'true' verdadero. 
 * el operador ':' pregunta si el el valor ingresado es 'false' falso.
 */
let edad = 18;
let acceso = edad >= 18 ? 'puedes ingresar' : 'lo sentimos, pero no cumples los requerimientos del acceso';
 console.log(acceso);
 // estos operadores se parecen mucho al operador de control de flujo 'if' pero no se deben confundir